framework:
    mailer:
        message_bus: false
    messenger:
        default_bus: messenger.bus.default
        buses:
            messenger.bus.default: ~
        transports:
            async: 'in-memory://'
        routing:
            App\Message\SendWelcomeMessage: async
