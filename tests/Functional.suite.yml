# Framework Testing
actor: FunctionalTester
modules:
    enabled:
        - Asserts
        - Symfony:
              app_path: 'src'
              environment: 'test'
        - Doctrine:
              depends: Symfony
              cleanup: true
